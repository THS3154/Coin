<UserControl x:Class="Setting.Views.DialogSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"        
             MinWidth="200"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <!--ColumnDefinition Width="1*"/-->
        </Grid.ColumnDefinitions>

        <!--Grid Grid.Column="0">
            <ListView ItemsSource="{Binding OptionList}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Button Command="{Binding DataContext.BtnMenuChangedCommand,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=UserControl}}" CommandParameter="{Binding Name}" Content="{Binding PrintName}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </Grid-->

        <Grid Grid.Column="0">
            <ContentControl  prism:RegionManager.RegionName="SettingMain" />
        </Grid>
    </Grid>
</UserControl>
